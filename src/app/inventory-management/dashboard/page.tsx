"use client";

/** Placeholder — the dashboard's contents get defined in a later pass. */
export default function InventoryDashboardPage() {
  return (
    <div className="max-w-3xl">
      <h1 className="text-2xl font-bold tracking-tight text-[#1c1c1c]">
        Dashboard
      </h1>
      <p className="mt-2 text-sm text-gray-600">Nothing here yet.</p>
    </div>
  );
}
